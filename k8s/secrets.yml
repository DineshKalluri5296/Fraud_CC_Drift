apiVersion: v1
kind: Secret

metadata:
  name: fraud-secret
  namespace: fraud

type: Opaque

stringData:

  AWS_REGION: us-east-1

  MODEL_BUCKET: fraud-model-bucket

  SECRET_KEY: replace-with-your-secret