apiVersion: v1

kind: Service

metadata:

  name: fraud-service

  namespace: fraud

  labels:

    app: fraud-api

spec:

  selector:

    app: fraud-api

  ports:

  - name: http

    port: 80

    targetPort: 8000

    protocol: TCP

  type: LoadBalancer